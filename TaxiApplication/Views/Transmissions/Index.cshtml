@model IEnumerable<TaxiApplication.Data.Transmission>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Owner.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.transName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.transName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.transID }) |
            @Html.ActionLink("Details", "Details", new { id=item.transID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.transID })
        </td>
    </tr>
}

</table>
