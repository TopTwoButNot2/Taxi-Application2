@model IEnumerable<TaxiApplication.Data.Reservation>

@{
    Layout = "~/Views/Shared/_Passenger.cshtml";
}
<br />
<br />
<br />
<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        @*<th>
                @Html.DisplayNameFor(model => model.GetAvailable.DriverName)
            </th>*@
        <th>
            @Html.DisplayNameFor(model => model.passengers.FirstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.schedule.ownerID)
        </th>

        <th>
            @Html.DisplayNameFor(model => model.picture)
        </th>

        <th>
            @Html.DisplayNameFor(model => model.status)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TotalPrice)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PassengerName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DepartureDate)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            @*<td>
                    @Html.DisplayFor(modelItem => item.GetAvailable.DriverName)
                </td>*@
            <td>
                @Html.DisplayFor(modelItem => item.passengers.FirstName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.schedule.ownerID)
            </td>

            <td>
                @*<a href="@Url.Action("Details", "VehicleMakes", new { id = item.vMakeID })" title="View Details" , rel="nofollow">
                        <img src="data:image/png;base64,
                             @Convert.ToBase64String(item.Image, 0, item.Image.Length)" style="width: 60px; height: 60px" alt="" />
                    </a>*@
                <img src="@Url.Action("RenderImage", "Reservations", new { id = item.BookingId})" width="50" height="60" />
            </td>

            <td>
                @Html.DisplayFor(modelItem => item.status)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TotalPrice)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PassengerName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DepartureDate)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.BookingId })|
                @Html.ActionLink("Details", "Details", new { id = item.BookingId }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.BookingId })
            </td>
        </tr>
    }

</table>
