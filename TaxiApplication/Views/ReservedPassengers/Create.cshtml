@model TaxiApplication.Data.TaxiPosition
 @using TaxiApplication.BusinessLogics

@{
    ViewBag.Title = "Create";
    Layout = "~/Views/Shared/_Passenger.cshtml";

    TaxiPositionLogics l = new TaxiPositionLogics();
}

<h2>Create</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

<div class="form-horizontal">
    <h4>ReservedPassengers</h4>
    <hr />
    @Html.ValidationSummary(true, "", new { @class = "text-danger" }) 
    <label>Select Taxi</label>
    <br />
    <select name="ID">
        @{

        foreach (var x in l.GetTaxiPositions(ViewBag.routename))
        {
        <option value="@x.ID"> @x.TaxiNo => @x.LoadingTime - @x.DepertureTime</option>
        }
        }
    </select>


    @*<div class="form-group">
        @Html.LabelFor(model => model.PassengerId, "PassengerId", htmlAttributes: new { @class = "control-label col-md-2" })
        <div class="col-md-10">
            @Html.DropDownList("PassengerId", null, htmlAttributes: new { @class = "form-control" })
            @Html.ValidationMessageFor(model => model.PassengerId, "", new { @class = "text-danger" })
        </div>
    </div>*@

    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" value="Create" class="btn btn-default" />
        </div>
    </div>
</div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
