@model IEnumerable<TaxiApplication.Data.TaxiPosition>


@{
    ViewBag.Title = "SelectRout";
    Layout = "~/Views/Shared/_Passenger.cshtml";

}

<h2>SelectRout</h2>

@using (Html.BeginForm("SelectRout", "ReservedPassengers", FormMethod.Get))
{
    @Html.TextBox("search") <input type="submit" value="search" />
}
<tbody>
    @if (Model.Count() == 0)
    {
        <tr>
            <td>
                No Match
            </td>
        </tr>
    }
    
</tbody>


@foreach (var item in Model)
{

    <tr>
        <td>
            AVAILABLE TAXIS = @Html.DisplayFor(modelItem => item.Count)

        </td>
    </tr>
    <tr>
        <td class="text-right text-nowrap">


            <div class="btn btn-github pull-right"> @Html.ActionLink("Select Route>", "Create", new { id = item.ID })  |</div>


           
        </td>

    </tr>

   



    @*<div class="container">
            <div class="panel panel-default">
                <table class="table table-hover">
                    <tbody>
                        <tr>
                            <td>
                                <span class="glyphicon glyphicon-file"></span>


                                @Html.DisplayFor(modelItem => item.RouteName)|
                                @Html.DisplayFor(modelItem => item.RoutePrice)|
                                @Html.DisplayFor(modelItem => item.LoadingTime)|
                                @Html.DisplayFor(modelItem => item.DepertureTime)


                            </td>
                            <td class="text-right text-nowrap">


                                <div class="btn btn-github pull-right"> @Html.ActionLink("Select Route>", "Create", new { id = item.ID })  |</div>


                                <button class="btn btn-xs btn-warning">
                                    <span class="glyphicon glyphicon-trash"></span>
                                </button>
                            </td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </div>*@



}
